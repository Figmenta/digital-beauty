/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ["db.figmenta.digital"], // Agrega el dominio de tus imágenes aquí
    },
  };
  
  export default nextConfig;